@page "/internal/promocodes"
@model Roblox.Website.Pages.Internal.Promocodes

@{
    Layout = "Shared/_Layout";
}

@section MetaTags {
    <title>Redeem BubbaBlox Promotions</title>
}

@section Styles {
    <style>
        body {
            background-color: #F4F5F7;
            font-family: 'Gotham', sans-serif;
            color: #00b16f;
        }
        button[value="redeem"] {
            background-color: #00b16f;
            color: #f2f4f5;
            border-radius: 10px;
            font-family: 'Gotham', sans-serif;
        }
        
        #code {
            border-radius: 6px;
            background-color: #00b16f;
            border: 4px solid #27292B;
        }
    </style>
}

<div class="container mb-4 mt-4">
    <div class="row">
        <div class="col-12">
            <div class="card card-body">
                <h3>Redeem BubbaBlox Promotions</h3>
                
                @if (Model.Result != null && Model.Result.showResult)
                {
                    @if (!Model.Result.success)
                    {
                        <div class="alert alert-danger">@Model.Result.message</div>
                    }
                    else
                    {
                        <div class="alert alert-success">@Model.Result.message</div>
                        
                        @if (Model.Result.assetId.HasValue)
                        {
                            <div class="mt-3">
                                <h4>You received:</h4>
                                <div class="row">
                                    <div class="col-6 col-md-4 col-lg-2 mb-2">
                                        <div class="card">
                                            <a href="/catalog/@Model.Result.assetId/--">
                                                <img class="w-100 mx-auto" src="/thumbs/asset.ashx?assetId=@Model.Result.assetId"/>
                                            </a>
                                            <div class="card-body">
                                                <p class="fw-bolder text-truncate mb-0">@(Model.Result.name ?? "Unknown Item")</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                        
                        @if (Model.Result.robux.HasValue)
                        {
                            <div class="mt-3">
                                <h4>You received:</h4>
                                <div class="row">
                                    <div class="col-6 col-md-4 col-lg-2 mb-2">
                                        <div class="card">
                                            <a href="#">
                                                <img class="w-100 mx-auto" src="@($"{Roblox.Configuration.BaseUrl}/UnsecuredContent/robux.png")" />
                                            </a>
                                            <div class="card-body">
                                                <p class="fw-bolder text-truncate mb-0">@Model.Result.robux.Value.ToString("N0") Robux</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                }
                
                <form method="post" class="mt-3">
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <input type="text" class="form-control" id="code" name="code" placeholder="" required>
                        <span asp-validation-for="Code" class="text-danger"></span>
                    </div>
                    <button type="submit" class="btn btn-primary mt-2" name="handler" value="redeem">
                        <span id="submitText">Redeem</span>
                        <span id="spinner" class="spinner-border spinner-border-sm d-none" role="status" aria-hidden="true"></span>
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.querySelector('form').addEventListener('submit', function() {
            const submitBtn = document.querySelector('button[type="submit"]');
            const submitText = document.getElementById('submitText');
            const spinner = document.getElementById('spinner');
            
            submitBtn.disabled = true;
            submitText.classList.add('d-none');
            spinner.classList.remove('d-none');
        });
    </script>
}

